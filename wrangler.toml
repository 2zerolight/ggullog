name = "ggullog"
# 이 계정에 ggullog 워커가 있습니다 (계정이 2개라 지정하지 않으면 배포 시 선택 프롬프트가 뜸)
account_id = "3a70dca9d2c01672845a5d4cbeb70b10"
compatibility_date = "2026-07-01"

[assets]
directory = "./site"
not_found_handling = "404-page"
html_handling = "auto-trailing-slash"

# 커스텀 도메인 연결은 Cloudflare 대시보드 > Workers > 설정 > 도메인 및 경로에서
# ggullog.com 을 추가하면 됩니다.
