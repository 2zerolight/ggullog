# 꿀로그(ggullog.com) 배포 및 애드센스 신청 가이드

이 문서 하나만 따라 하시면 됩니다. 코딩 지식이 없어도 괜찮습니다. 명령어는 그대로 복사해서 붙여넣기만 하세요.

---

## 0. 받으신 파일 구성

| 폴더/파일 | 설명 |
| --- | --- |
| `site/` | 완성된 웹사이트 전체 (이 폴더가 실제로 배포됩니다) |
| `posts/` | 글 원본(Markdown). 나중에 글을 추가할 때 사용 |
| `pages/` | 소개·연락처·개인정보처리방침·이용약관 원본 |
| `build.py` | 원본(Markdown)을 웹사이트(HTML)로 변환하는 프로그램 |
| `wrangler.toml` | Cloudflare Workers 배포 설정 파일 |
| `배포가이드.md` | 지금 보고 계신 이 문서 |

압축 파일(`ggullog-site.zip`)을 받으셨다면, 먼저 압축을 풀고 그 폴더를 `문서(Documents)` 아래에 두세요. 기존에 `/Users/20k/Documents/ggullog` 폴더가 있다면 안의 내용을 이 파일들로 교체하시면 됩니다.

---

## 1. Cloudflare Workers에 배포하기 (Mac 기준)

### 1-1. 터미널 열기

Mac에서 `command + 스페이스바`를 누르고 "터미널"을 입력한 뒤 Enter를 누릅니다.

### 1-2. Node.js 설치 확인

터미널에 아래를 입력하고 Enter:

```
node -v
```

`v18` 이상 버전 숫자가 나오면 통과입니다. "command not found"가 나오면 https://nodejs.org 에서 LTS 버전을 다운로드해 설치한 뒤 터미널을 껐다 다시 켜세요.

### 1-3. 프로젝트 폴더로 이동

```
cd /Users/20k/Documents/ggullog
```

(폴더 위치가 다르면 그 경로로 바꿔주세요.)

### 1-4. Cloudflare 로그인

```
npx wrangler login
```

브라우저가 열리면서 Cloudflare 로그인 화면이 나옵니다. 로그인하고 "Allow(허용)"를 누르세요. Cloudflare 계정이 없다면 https://dash.cloudflare.com/sign-up 에서 무료로 만들면 됩니다.

### 1-5. 배포 (핵심!)

```
npx wrangler deploy
```

몇 초 뒤 `https://ggullog.xxxx.workers.dev` 형태의 주소가 출력되면 성공입니다. 그 주소로 접속해 사이트가 잘 보이는지 확인하세요.

### 1-6. 도메인(ggullog.com) 연결

애드센스 심사는 `workers.dev` 무료 주소보다 **본인 도메인으로 신청하는 것이 훨씬 유리**합니다.

1. 먼저 ggullog.com 도메인이 Cloudflare에 등록되어 있어야 합니다. (Cloudflare 대시보드 → "도메인 추가"에서 ggullog.com 추가 → 안내에 따라 네임서버 변경. 이미 Cloudflare에서 산 도메인이라면 이 단계는 자동으로 되어 있습니다.)
2. Cloudflare 대시보드(https://dash.cloudflare.com) 접속 → 왼쪽 메뉴 **Workers 및 Pages** → `ggullog` 클릭
3. **설정(Settings)** 탭 → **도메인 및 경로(Domains & Routes)** → **추가(Add)** → **사용자 지정 도메인(Custom Domain)**
4. `ggullog.com` 입력 후 추가. 같은 방법으로 `www.ggullog.com`도 추가
5. 몇 분 뒤 https://ggullog.com 으로 접속되면 완료

---

## 2. 애드센스 신청 전 체크리스트

배포가 끝났다면, 신청 전에 아래를 순서대로 진행하세요.

### 2-1. Google Search Console 등록 (강력 권장)

구글이 사이트를 인식하게 만드는 과정입니다. 승인 확률을 높여줍니다.

1. https://search.google.com/search-console 접속 → "시작하기"
2. "도메인" 방식으로 `ggullog.com` 입력
3. 안내에 나오는 TXT 레코드를 Cloudflare 대시보드 → ggullog.com → **DNS** → **레코드 추가**에서 추가 (유형: TXT, 이름: @, 내용: 구글이 준 값)
4. Search Console로 돌아와 "확인" 클릭
5. 확인이 되면 왼쪽 메뉴 **Sitemaps**에서 `https://ggullog.com/sitemap.xml` 을 제출

### 2-2. 2~4주 정도 사이트 숙성시키기 (중요)

만든 즉시 신청하는 것보다, **사이트를 배포한 뒤 2~4주 정도 기다렸다가 신청하는 것이 승인율이 훨씬 높습니다.** 이 기간 동안 구글이 사이트를 색인하고, 자연스러운 운영 이력이 쌓입니다. 기다리는 동안 일주일에 1~2편씩 글을 직접 추가하면 더욱 좋습니다(방법은 4번 참고).

### 2-3. 이메일 주소 준비

연락처 페이지에 `contact@ggullog.com`이 적혀 있습니다. 이 주소가 실제로 수신되도록 설정하세요. Cloudflare 대시보드 → ggullog.com → **이메일(Email)** → **이메일 라우팅(Email Routing)**에서 `contact@ggullog.com`으로 오는 메일을 본인 Gmail로 전달하도록 무료로 설정할 수 있습니다. 만약 다른 이메일을 쓰고 싶다면 `pages/contact.md` 파일에서 주소를 바꾸고 다시 빌드·배포하세요.

---

## 3. 애드센스 신청하기

1. https://adsense.google.com 접속 → 시작하기 → 사이트 주소에 `ggullog.com` 입력
2. 애드센스가 **"사이트에 코드를 붙여넣으세요"**라며 `<script ...adsbygoogle.js?client=ca-pub-숫자...>` 형태의 코드를 줍니다.
3. 받은 코드에서 `ca-pub-` 뒤의 숫자(게시자 ID)를 복사한 뒤, 컴퓨터에서 `build.py` 파일을 텍스트 편집기로 열어 아래 부분을 찾습니다:

```
<!-- <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-XXXXXXXXXXXXXXXX" crossorigin="anonymous"></script> -->
```

앞뒤의 `<!--` 와 `-->` 를 지우고, `ca-pub-XXXXXXXXXXXXXXXX`를 본인 ID로 바꿉니다. 예:

```
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-1234567890123456" crossorigin="anonymous"></script>
```

4. `site/ads.txt` 파일도 열어서, 안내된 대로 `#`을 지우고 본인의 게시자 ID로 교체합니다:

```
google.com, pub-1234567890123456, DIRECT, f08c47fec0942fa0
```

5. 터미널에서 다시 빌드하고 배포합니다:

```
cd /Users/20k/Documents/ggullog
pip3 install markdown
python3 build.py
npx wrangler deploy
```

(참고: `build.py`를 실행하면 `ads.txt`가 초기 상태로 되돌아가므로, **빌드 후에** `site/ads.txt`를 수정하고 배포하거나, 4번을 먼저 하고 3번 순서로 진행하세요.)

6. 애드센스 화면으로 돌아가 "검토 요청"을 누릅니다. 심사는 보통 며칠에서 2~4주 걸립니다.

---

## 4. 새 글 추가하는 방법 (승인 전후 공통)

1. `posts/` 폴더의 아무 `.md` 파일이나 복사해서 새 파일을 만듭니다. 예: `saving-new-tip.md`
2. 파일 맨 위 `---` 사이의 정보(제목, slug, 카테고리, 날짜, 설명)를 새 글에 맞게 수정합니다. slug는 파일명과 똑같이 맞춰주세요.
3. 그 아래에 글 본문을 Markdown으로 작성합니다. (`##` = 소제목)
4. 터미널에서:

```
cd /Users/20k/Documents/ggullog
python3 build.py
npx wrangler deploy
```

이 두 명령이면 사이트에 즉시 반영됩니다.

---

## 5. 승인 확률을 높이는 운영 수칙

| 하세요 | 하지 마세요 |
| --- | --- |
| 배포 후 2~4주 숙성 뒤 신청 | 만들자마자 바로 신청 |
| 주 1~2편 꾸준히 글 추가 | AI로 하루 수십 편 대량 발행 |
| 본인 경험을 글에 조금씩 녹이기 | 다른 블로그 글 복사 |
| 거절되면 사유 확인 후 글 보강 → 2주 뒤 재신청 | 거절 직후 바로 재신청 반복 |
| 승인 후 광고 코드 추가 | 심사 중 사이트 구조 대대적 변경 |

**거절되더라도 절대 끝이 아닙니다.** 대부분 "콘텐츠 부족/저품질" 사유이며, 글을 5~10편 더 추가하고 2주 뒤 재신청하면 통과되는 경우가 많습니다. 거절 메일을 받으시면 그 내용을 저(Manus)에게 보여주세요. 함께 보완하겠습니다.

---

## 6. 문제가 생겼을 때

| 증상 | 해결 |
| --- | --- |
| `wrangler: command not found` | `npx wrangler deploy` 처럼 앞에 `npx`를 꼭 붙이세요 |
| `python3: No module named markdown` | `pip3 install markdown` 실행 후 다시 시도 |
| 배포는 됐는데 사이트가 안 보임 | 5분 정도 기다린 뒤 새로고침, 브라우저 캐시 삭제 |
| 도메인 연결이 안 됨 | Cloudflare에 도메인의 네임서버가 연결됐는지 확인 (도메인 개요 화면에 "활성" 표시) |

여기까지 하시면 애드센스 심사를 받을 준비가 모두 끝납니다. 응원합니다!
